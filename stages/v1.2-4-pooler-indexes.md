# v1.2-4 — pooler env + индексы БД

## Env / docs
- `.env.example`: `DATABASE_POOLER_URL`, `DATABASE_URL`, `SUPABASE_DB_URL`
- `docs/SUPABASE-DATABASE.md` — как взять URI из Dashboard
- `lib/supabase/database-env.ts` — хелперы для CLI

## Клиент Supabase
- `lib/supabase/fetch.ts` — таймаут 25s, keepalive
- Подключён в `server.ts`, `middleware.ts`, `admin.ts`

## Миграция
- `20260522140000_schedule_perf_indexes.sql` — составные индексы lessons / participants

## Скрипты
- `npm run db:push` / `db:status`
