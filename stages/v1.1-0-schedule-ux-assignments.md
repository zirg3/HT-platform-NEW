# v1.1 — расписание, привязки, админ-преподаватель

## БД
- Миграция `20260521120000_v11_lessons_profiles.sql`: `profiles.is_teacher`, `lessons.cancellation_reason`, `rescheduled_at`, `rescheduled_by`, `original_starts_at`

## Расписание
- Пастельные цвета слотов: будущий / отменён / перенесён (`lesson-appearance.ts`, `lesson-block.tsx`)
- Увеличена высота слота (`SCHEDULE_SLOT_PX=64`, `week-calendar.tsx`)
- Отмена с полем «Причина» (`schedule.ts`, `lesson-dialog.tsx`)
- Перенос урока: дата/время, права admin/manager/teacher своих (`rescheduleLessonAction`, UI в диалоге)
- «Провести урок» только после `starts_at` (`canCompleteLessonNow`)
- Списки ЛК: до 12 карточек, grid «как таблица» (`agenda-lesson-lists.tsx`, `AGENDA_LIMIT`)

## Админка
- Редактирование привязки ученик↔преподаватель (`updateAssignmentAction`, `assignments-manager.tsx`)
- Флаг «Также преподаватель» у профиля (`is_teacher`, `users.ts`, `users-manager.tsx`, `fetchTeachers`)

## Преподаватель
- История отмен в профиле ученика (`fetchCancelledLessonsForStudent`, страница `[studentId]`)

## Прочее
- `formKey` в `lesson-dialog` — controlled FieldControl (Base UI warning)
- `supabase/README.md` — миграция v1.1
