# v1.2-3 — производительность расписания

## Запросы
- `LESSON_CALENDAR_SELECT` / `LESSON_AGENDA_SELECT` / `LESSON_CONFLICT_SELECT` — меньше join-ов
- Agenda: окно 14+45 дней, limit 80 (было 90+180 без limit)
- Преподаватель: убран лишний `fetchAllStudents`
- Создание урока: `fetchLessonsForWeekConflicts` вместо полной недели
- `React.cache` (не `unstable_cache` — иначе конфликт с `cookies()` / RLS)
- `revalidatePath` по роли после мутаций
- `getSessionProfile` — `React.cache` (layout + page)

## UI
- `Suspense` + skeleton для agenda (календарь раньше)
- `loading.tsx` teacher/student

## Сеть
- retry 4× при ECONNRESET
