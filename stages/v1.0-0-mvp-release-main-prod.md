# v1.0-0 — релиз MVP на main/prod

## Что сделано

- `dev-0.7` закоммичен и запушен (`b79d3d6`).
- Fast-forward merge в **`dev`**, push `origin/dev`.
- **`main`** и **`prod`** выровнены на `dev` (`reset --hard` + `push --force`) — единое дерево MVP v1.0.

## Проверка после деплоя

- Auth, 4 роли, RLS
- Расписание, CRUD, отмена, серии
- Восстановление пароля (Redirect URL в Supabase)
- Админ: пользователи, курсы, привязки
