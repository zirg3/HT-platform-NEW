# dev-react-8 — aurora: голубой верх-справа + диагональная полоса

## Изменения

- **Правый верхний угол**: radial `94% 6%` — голубой оттенок как у пятна справа по центру (`210` hue).
- **Белая линия**: убрана горизонтальная полоса; одна **диагональная** (`128deg`), шире (~43–57% вместо 49–51%).
- **Голубой за аватаром**: radial `97% -4%` (выше topbar) + accent `100% -3%`. Центр справа (`92% 62%`) без изменений.

## Проверка

`npm run dev` — правый верх заметно голубее; светлая полоса по диагонали через центр.
