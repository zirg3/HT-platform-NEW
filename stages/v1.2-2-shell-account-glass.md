# v1.2-2 — shell Glass Aurora + аккаунт

## Shell
- `aurora-bg` + отступы; sidebar и main в отдельных glass-контейнерах со скруглением `rounded-3xl`
- `glass-main` — правая панель контента
- Sidebar: плавающий, скруглённый, отступ от края

## Аккаунт
- `account-menu.tsx`: выпадающее меню по клику на имя/роль, «Выйти» внутри
- Убран колокольчик
- Аватар в шапке: `variant="photo"` без овального glass-фона

## UI
- Бордеры полей: `glass-input` + `border-input`
- Календарь: `divide-x` — вертикальные разделители дней
- Исправлен `Button asChild` → `Link` + `buttonVariants` (warning React)

## Supabase
- `withFetchRetry` для courses/lessons/profiles при ECONNRESET
