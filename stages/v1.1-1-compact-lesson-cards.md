# v1.1.1 — компактные карточки уроков

## UI
- `compact-lesson-card.tsx` — общий вид: курс, время·участник, статус (как в календаре)
- Списки «Предстоящие/Прошедшие»: без подписей Курс/Дата/Ученик, сетка мельче (до 6 колонок)
- Календарь: 2 строки в ячейке, `overflow-hidden`, без `min-h-[56px]` — блок не вылезает за слот
- Профиль ученика у teacher: `StudentProfileAgenda` + модалка урока
- `lessonContext` в `WeekCalendar` для ученика (преподаватель) / teacher (ученик)

## Fix
- Вход: `loginAction` возвращает `redirectTo` вместо `redirect()` — совместимость с `useActionState`
- Проведённые уроки: серый (`slate`), будущие — зелёный (`emerald`)
- Перенос: панель закрывается после сохранения; «К ученику» внутри карточки (общий фон, разделитель)
