# v0.07-2 — шапка календаря и мелкий UX

## Что сделано

- **Шапка календаря**: выбор недели по центру; на десктопе — `←` слева, «Сегодня» и `→` справа; на мобильном — дата сверху, навигация строкой ниже.
- **WeekDatePicker**: обрезка длинного диапазона дат на узком экране (`truncate`).
- **Календарь**: `aria-label` на горизонтальной прокрутке сетки.
- **LessonDialog**: сброс формы при открытии без `useEffect` (исправлен lint).
- **supabase/README.md**: инструкция по Redirect URL для восстановления пароля.
